import {StyleSheet} from 'react-native';
import {theme} from '../../../theme/theme';

export const styles = StyleSheet.create({
  outerView: {paddingBottom: 125, backgroundColor: theme.BackgroundScreen},
});
