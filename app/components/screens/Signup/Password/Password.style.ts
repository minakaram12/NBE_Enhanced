import {StyleSheet} from 'react-native';
import {theme} from '../../../../theme/theme';

const styles = StyleSheet.create({
  container: {backgroundColor: theme.BackgroundMenu},
});

export default styles;
